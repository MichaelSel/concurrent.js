function greet(name: string): string {
  return `Hello, ${name}!`;
}

// Example function call
const greeting = greet('world');
console.log(greeting);


// You can edit this file, save it, and see changes automatically without manual recompilation